import { FC } from 'react';
import NextHead from 'next/head';

const Head: FC = () => (
  <NextHead>
    <title> Thanh Minh portfolio</title>
    <meta name="viewport" content="width=device-width, initial-scale=1" />
  </NextHead>
);

export default Head;
